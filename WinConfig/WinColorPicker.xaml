<Window x:Class="TypeSunny.WinColorPicker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="颜色选择" Height="280" Width="400" Icon="/Resources/logo-512.png"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="选择颜色" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- 十六进制输入 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="十六进制颜色值：" VerticalAlignment="Center" Width="120"/>
            <TextBox x:Name="TbxHexColor" Width="150" Height="28" VerticalContentAlignment="Center"
                     TextChanged="TbxHexColor_TextChanged" MaxLength="7"/>
            <TextBlock Text="(支持 #F7F7F7 或 F7F7F7)" VerticalAlignment="Center"
                       Margin="10,0,0,0" FontSize="11" Foreground="Gray"/>
        </StackPanel>

        <!-- 颜色预览 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="颜色预览：" VerticalAlignment="Center" Width="120"/>
            <Border x:Name="ColorPreview" Width="150" Height="40" BorderBrush="Gray" BorderThickness="1">
                <Border.Background>
                    <SolidColorBrush x:Name="PreviewBrush" Color="White"/>
                </Border.Background>
            </Border>
        </StackPanel>

        <!-- 标准颜色选择器按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="或使用拾色器：" VerticalAlignment="Center" Width="120"/>
            <Button x:Name="BtnColorPicker" Content="打开颜色拾色器" Width="150" Height="30"
                    Click="BtnColorPicker_Click"/>
        </StackPanel>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnOK" Content="确定" Width="80" Height="30" Margin="0,0,10,0"
                    Click="BtnOK_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Content="取消" Width="80" Height="30"
                    Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
