<Window x:Class="TypeSunny.WinArticle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TypeSunny"
        mc:Ignorable="d"
        Title="本地文章管理器"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResize"
        Background="Transparent"
        Icon="/Resources/ico/sunny.ico"
        xml:lang="zh-CN"
        Width="800" Height="450"
        SizeToContent="Manual"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- 主题颜色资源（默认亮色，运行时由ApplyThemeColors覆盖） -->
        <SolidColorBrush x:Key="WindowBackground" Color="#FFF7F7F7"/>
        <SolidColorBrush x:Key="WindowBorderBrush" Color="#FFC7C7C7"/>
        <SolidColorBrush x:Key="TextForeground" Color="#FF606060"/>
        <SolidColorBrush x:Key="ToolbarBackground" Color="#FFE8E8E8"/>
        <SolidColorBrush x:Key="ContentBackground" Color="#FFEDEDED"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FFC7C7C7"/>
        <SolidColorBrush x:Key="ButtonBackground" Color="#FFEBEBEB"/>
        <SolidColorBrush x:Key="ButtonHoverBackground" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="ButtonPressedBackground" Color="#FFCCCCCC"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF007ACC"/>

        <!-- 现代化按钮样式 -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonPressedBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 数值输入框样式 -->
        <Style x:Key="NumberInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ToolbarBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 数值调节按钮样式 -->
        <Style x:Key="NumberButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 标题栏按钮样式 -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 关闭按钮样式（右上角圆角） -->
        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0,4,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="2" Color="Black" Opacity="0.3"/>
    </Window.Effect>

    <Grid KeyboardNavigation.TabNavigation="Cycle">
        <!-- 外层边框 -->
        <Border x:Name="MainBorder"
                BorderThickness="1"
                BorderBrush="{DynamicResource WindowBorderBrush}"
                CornerRadius="6"
                ClipToBounds="True"
                Margin="5"
                Background="{DynamicResource WindowBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="28"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!-- 自定义标题栏 -->
                <Border Grid.Row="0" CornerRadius="5,5,0,0" ClipToBounds="True" Margin="2,2,2,0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                            <Image x:Name="TitleBarIcon" Source="/Resources/ico/sunny.ico" Width="14" Height="14" Margin="0,0,6,0"/>
                            <TextBlock Text="本地文章管理器" FontSize="11" Foreground="{DynamicResource TextForeground}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="BtnMinimize" Content="━" Click="BtnMinimize_Click" Style="{StaticResource TitleBarButtonStyle}"/>
                            <Button x:Name="BtnClose" Content="×" Click="BtnClose_Click" Style="{StaticResource TitleBarCloseButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 工具栏 -->
                <Border Grid.Row="1" Background="{DynamicResource ToolbarBackground}" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderBrush}" Padding="10,6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <ComboBox x:Name="CbFiles" Width="120" Height="26" SelectionChanged="CbFiles_SelectionChanged"/>
                            <Button Content="刷新" Margin="8,0,0,0" Click="BtnReload_Click" Style="{StaticResource ModernButtonStyle}"/>
                            <Button Content="打开目录" Margin="5,0,0,0" Click="BtnOpen_Click" Style="{StaticResource ModernButtonStyle}"/>
                            <Button Content="发送" Margin="5,0,0,0" Click="BtnSend_Click" Style="{StaticResource ModernButtonStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <CheckBox Name="CbFilter" VerticalAlignment="Center" Checked="CbFilter_Checked" Unchecked="CbFilter_Unchecked" Foreground="{DynamicResource TextForeground}">
                                <TextBlock Text="字集过滤" Foreground="{DynamicResource TextForeground}" FontSize="12"/>
                            </CheckBox>
                            <CheckBox Name="CbRemoveSpace" VerticalAlignment="Center" Margin="10,0,0,0" Checked="CbRemoveSpace_Checked" Unchecked="CbRemoveSpace_Unchecked" Foreground="{DynamicResource TextForeground}">
                                <TextBlock Text="去除空格" Foreground="{DynamicResource TextForeground}" FontSize="12"/>
                            </CheckBox>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 文章内容区 -->
                <Grid Grid.Row="2" Margin="10,8,10,8" Background="{DynamicResource ContentBackground}">
                    <TextBox x:Name="TbTest" Padding="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextWrapping="Wrap" Foreground="{DynamicResource TextForeground}" VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="Transparent" FontSize="15"/>
                </Grid>

                <!-- 导航栏 -->
                <Grid Grid.Row="3" Margin="10,4,10,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="◀" Click="BtnPrev_Click" Style="{StaticResource ModernButtonStyle}" Padding="8,4"/>
                    <Button Grid.Column="1" Content="▶" Margin="4,0,8,0" Click="BtnNext_Click" Style="{StaticResource ModernButtonStyle}" Padding="8,4"/>
                    <Slider x:Name="SldProgress" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Minimum="1" IsSnapToTickEnabled="True" TickPlacement="None" ValueChanged="SldProgress_ValueChanged" TickFrequency="1" Opacity="0.7"/>
                    <TextBox x:Name="TbSearch" Grid.Column="3" Width="140" Height="26" Margin="8,0,4,0" Background="{DynamicResource ToolbarBackground}" Foreground="{DynamicResource TextForeground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="4,2" VerticalContentAlignment="Center" PreviewKeyDown="TbSearch_PreviewKeyDown"/>
                    <Button Grid.Column="4" Content="搜索" Click="BtnSearch_Click" Style="{StaticResource ModernButtonStyle}"/>
                </Grid>

                <!-- 每段字数设置面板 -->
                <Grid Grid.Row="4" HorizontalAlignment="Center" Margin="0,4,0,10">
                    <Border Background="{DynamicResource ToolbarBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10,8">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="每段字数" Foreground="{DynamicResource TextForeground}" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox x:Name="TbSectionSize" Width="60" Text="200" Style="{StaticResource NumberInputStyle}" InputMethod.IsInputMethodEnabled="False" TextChanged="TbSectionSize_TextChanged"/>
                            <StackPanel Orientation="Horizontal" Margin="4,0,0,0" VerticalAlignment="Center">
                                <Button Content="▲" Click="SectionSizeUp" Style="{StaticResource NumberButtonStyle}" Margin="0,0,2,0"/>
                                <Button Content="▼" Click="SectionSizeDown" Style="{StaticResource NumberButtonStyle}"/>
                            </StackPanel>
                            <TextBlock Text="字/段" Foreground="{DynamicResource TextForeground}" FontSize="11" VerticalAlignment="Center" Margin="6,0,0,0" Opacity="0.7"/>
                            <TextBlock Text="换段模式" Foreground="{DynamicResource TextForeground}" FontSize="12" VerticalAlignment="Center" Margin="18,0,8,0"/>
                            <ComboBox x:Name="CbSegmentMode" Width="72" Height="26" SelectionChanged="CbSegmentMode_SelectionChanged">
                                <ComboBoxItem Content="自动"/>
                                <ComboBoxItem Content="手动"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Resize边框 -->
        <Border Name="ResizeTop" Background="Transparent" Height="6" VerticalAlignment="Top" Cursor="SizeNS" MouseLeftButtonDown="ResizeBorder_MouseLeftButtonDown"/>
        <Border Name="ResizeBottom" Background="Transparent" Height="6" VerticalAlignment="Bottom" Cursor="SizeNS" MouseLeftButtonDown="ResizeBorder_MouseLeftButtonDown"/>
        <Border Name="ResizeLeft" Background="Transparent" Width="6" HorizontalAlignment="Left" Cursor="SizeWE" MouseLeftButtonDown="ResizeBorder_MouseLeftButtonDown"/>
        <Border Name="ResizeRight" Background="Transparent" Width="6" HorizontalAlignment="Right" Cursor="SizeWE" MouseLeftButtonDown="ResizeBorder_MouseLeftButtonDown"/>
    </Grid>
</Window>
